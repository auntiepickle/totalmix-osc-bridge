[pytest]
testpaths = tests
# pytest's own defaults plus tools/ and agent/: tools/manual scripts act at import
norecursedirs = .* build dist node_modules venv .venv tools agent
# Fail fast on hangs — nothing in the suite should take anywhere near this
timeout = 60

# Required
OSC_IP=192.168.1.50          # IP address of the machine running TotalMix FX

# MQTT Settings (match your mosquitto setup)
MQTT_BROKER=mosquitto
MQTT_PORT=1883
MQTT_USER=your_mqtt_username
MQTT_PASS=your_mqtt_password
# === OSC MONITOR (for capturing real TotalMix addresses) ===
ENABLE_OSC_MONITOR=true
OSC_MONITOR_PORT=9001          # ← TotalMix default outgoing port

# Optional - change if you want custom topics
# MQTT_TOPIC_WORKSPACE=totalmix/workspace
# MQTT_TOPIC_SNAPSHOT=totalmix/snapshot